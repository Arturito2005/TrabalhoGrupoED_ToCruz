package TestTurnos;

import Jogo.Simulacao;
import Mapa.Divisao;
import Personagens.Inimigo;
import Cenarios.Divisao.CenariosDivisao;
import Cenarios.Personagens.CenariosInimigos;
import Cenarios.Personagens.CenariosPersonagem;
import org.junit.jupiter.api.BeforeEach;

public class TestTurno {
    /*
    * private CenariosPersonagem cenarioPersonagens;
    private CenariosDivisao cenariosDivisao;
    private Divisao divisaoAtual;

    @BeforeEach
    public void setUp() {

        cenarioPersonagens = new CenariosInimigos(new Simulacao());
        personagem2 = new Inimigo(2,"Legolas", 100, 60);
        personagem3 = new Inimigo(3, "Aragorn", 100, 50);
    }

    @Test
    void testConstructorAndGetters() {
        // Verifica se as dependências são inicializadas corretamente
        assertSame(cenarioPersonagensMock, turno.getCenarioPersonagens());
        assertSame(cenariosDivisaoMock, turno.getCenariosDivisao());
    }

    @Test
    void testTurnoMethodInvocation() {
        // Verifica se o método turno() pode ser chamado sem erros
        assertDoesNotThrow(() -> turno.turno(divisaoAtualMock));
    }
    * */
}
